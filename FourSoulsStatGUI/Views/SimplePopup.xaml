<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:local="clr-namespace:FourSoulsStatGUI"
               x:Class="FourSoulsStatGUI.SimplePopup"
               x:DataType="local:PopupViewModel">

    <Grid RowDefinitions="*, Auto">

        <CollectionView ItemsSource="{Binding Messages}" VerticalOptions="Center">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="OK" Grid.Row="1"
                Clicked="OnOKButtonClicked"></Button>
    </Grid>

</toolkit:Popup>

